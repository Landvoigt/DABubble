import { ReactionCountPipe } from './reaction-count.pipe';

describe('ReactionCountPipe', () => {
  it('create an instance', () => {
    const pipe = new ReactionCountPipe();
    expect(pipe).toBeTruthy();
  });
});
